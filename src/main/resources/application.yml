

spring:

  h2:
    console:
      enabled: true
      path: /h2-console

  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:~/fh-test
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect:
          format_sql: true
    logging.level:
      org.hibernate.SQL: debug
server:
  port: 8083

cloud:
  aws:
    s3:
      bucket: fh-image-bucket
      stack.auto: false
    region.static: ap-northeast-2

    credentials:
      accessKey: AKIAWDXS37LJXUP65Z5N
      secretKey: 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
